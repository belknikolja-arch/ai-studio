{
  "name": "ai-studio",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "description": "ИИ-Студия: чат-ИИ, который умеет всё — текст (LLM), фото, музыка, голос. Свои ключи API, без сервера.",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "test": "node --test tests/"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.8"
  }
}
